package com.example.manager.timetable;

/**
 * Enum representing different types of timetable generation solvers.
 */
public enum SolverType {
    /**
     * Simple solver with basic constraint satisfaction
     */
    SIMPLE,
    
    /**
     * Advanced Choco solver with complex constraint satisfaction
     */
    CHOCO
}
